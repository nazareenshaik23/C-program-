#include<stdio.h>
int fact(int);
int fact(int a)
{
    if(a==1)
    return 1;
    else
    return a*fact(a-1);
}
void main()
{
 int a;
 printf("enter any number");
 scanf("%d",&a);
 printf("factorial of given number is %d",fact(a));
}
